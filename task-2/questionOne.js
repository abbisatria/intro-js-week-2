let name = ['Abbi', 'Satria', 'Soemingkar']
// 1. join (converting array to string)
// console.log(name.join(' '));

// 2. pop (delete the array value at the last index)
// name.pop();
// console.log(name);

// 3. shift (increments the array value at the first index)
// name.unshift('Adji');
// console.log(name);

// 4. shift (delete the array value at the first index)
// name.shift();
// console.log(name);

// 5. splice (inserts an array element in the middle)
// ex : splice(firstIndex, wantToDelete, newElement, newElemen2, ...)
// name.splice(1, 2, 'Adji', 'Arti');
// console.log(name);

// 6. slice (slicing an array into a new array)
// ex: slice(awal,akhir);
// let name2 = name.slice(0,2);
// console.log(name2);

// 7. foreach (a loop that has a parameter in the form of a callback function)
// name.forEach(function(e) {
//  console.log(e);
// })

// 8. find (only finds one value and returns it as a string)
// let name2 = name.find(function(x) {
//  return x.length > 2;
// });
// console.log(name2);

// 9. toString (converts array to string from array values ​​(separated by commas))
// let name2 = name.toString()
// console.log(name2)

// 10. concat (create a new array by concatenating an existing array)
// let name2 = ['Arti', 'Ayu', 'Tasya']
// let name3 = name.concat(name2)
// console.log(name3)